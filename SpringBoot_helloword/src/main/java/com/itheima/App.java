package com.itheima;

/**
 * SpringBoot概述：SpringBoot的出现是为了解决Spring的存在的一下弊端，提供更快速使用spring的方式
 *        Spring的组件代码虽是轻量级的，但是它的配置和依赖管理却是比较繁重的事情。
 *        SpringBoot对spring的一些弊端进行了改善和优化，提高开发项目。
 *
 *        SpringBoot功能:
 *           自动配置：
 *               SpringBoot的自动配置就是在运行过程中，根据实际的工程需要，自动去配置spring和使用哪些组件等。
 *               无需再XML配置。

 *           起步依赖：
 *              SpringBoot提供了固化的starter起步依赖，简化依赖的管理m、maven导入坐标的过程。
 *              这些起步依赖就是将具有某些特定功能的坐标打包到一起，并提供默认的功能。
 *
 *             *辅助共功：
 *                  内部嵌入了服务器（如tomcat）,安全，指标，健康检查，外部配置等。
 *
 *        * SpringBoot在创建项目时，使用的是jar打包方式。
 *          SpringBoot的引导类，是项目的入口，运行main方法就可以启动项目。
 *
 *
 *
 */
public class App {
}
